<h1>Register New Team</h1>

<%= form_with url: "/competitions/#{params[:competition_id]}/teams", method: :post, local: true do |f| %>
  <%= f.label :hometown %>
  <%= f.text_field :hometown %>
  <%= f.label :nickname %>
  <%= f.text_field :nickname %>
  <%= f.submit 'submit' %>
<% end %>
